---
import ExperienceBentoStatic from "@/components/sections/experience/ExperienceBentoStatic.astro";
import { Skills } from "@/components/sections/skills";
import SocialIcons from "@/components/shared/SocialIcons.astro";
import { siteConfig } from "@/data/site.config";
import SiteLayout from "@/layouts/SiteLayout.astro";
import { generateBreadcrumbSchema, generateFAQPageSchema } from "@/lib/schema";
import { baseUrl } from "@/lib/seo";

const aboutUrl = `${baseUrl}/about`;

const aboutPageSchema = {
  "@context": "https://schema.org",
  "@type": "AboutPage",
  "@id": aboutUrl,
  mainEntity: {
    "@id": `${baseUrl}/#author`,
  },
  name: `About ${siteConfig.name}`,
  description: siteConfig.seo.metaDescription,
  url: aboutUrl,
};

const breadcrumbSchema = generateBreadcrumbSchema([
  { name: "Home", item: baseUrl },
  { name: "About", item: aboutUrl },
]);

const aboutFaqSchema = generateFAQPageSchema([...siteConfig.faq], aboutUrl);
---

<SiteLayout
  title={`About ${siteConfig.name} — Software Engineer & Builder`}
  description={siteConfig.seo.metaDescription}
>
  <script is:inline type="application/ld+json" set:html={JSON.stringify(aboutPageSchema)} />
  <script is:inline type="application/ld+json" set:html={JSON.stringify(breadcrumbSchema)} />
  <script is:inline type="application/ld+json" set:html={JSON.stringify(aboutFaqSchema)} />

  <article class="min-w-0">
    <section class="text-band pt-[var(--space-2xl)] pb-[var(--space-2xl)]" aria-labelledby="about-heading">
      <p class="mast-line mb-[var(--space-sm)]">About</p>

      <div
        class="grid gap-[var(--space-xl)] md:grid-cols-[minmax(0,2fr)_minmax(0,1fr)] md:items-start"
      >
        <div class="min-w-0 space-y-[var(--space-lg)]">
          <header class="space-y-[var(--space-md)]">
            <h1
              id="about-heading"
              class="font-display text-[length:var(--text-display-s)] leading-[1.05] text-foreground min-w-0 overflow-wrap-anywhere"
            >
              About me
            </h1>
            <p class="text-lg md:text-xl text-muted-foreground leading-relaxed prose-measure">
              {siteConfig.tagline}
            </p>
            <div class="accent-line !mt-[var(--space-md)] !mb-0" aria-hidden="true"></div>
          </header>

          <p class="prose-measure text-[length:var(--text-md)] leading-relaxed text-foreground">
            {siteConfig.about}
          </p>

          <p class="prose-measure text-base leading-relaxed text-foreground/90">
            Recent work includes <a href="https://github.com/cloudflare/vinext" class="text-primary link-underline-grow">Vinext</a> at Cloudflare (Next.js on Vite for Workers), <a href="https://github.com/Divkix/Logwell" class="text-primary link-underline-grow">LogWell</a> for self-hosted logging, <a href="https://pickmyclass.app" class="text-primary link-underline-grow">PickMyClass</a> for ASU course tracking, <a href="https://clickfolio.me" class="text-primary link-underline-grow">Clickfolio</a> for AI portfolios on Workers, and <a href="https://github.com/Divkix/Alita_Robot" class="text-primary link-underline-grow">Alita Robot</a> for Telegram communities ({siteConfig.facts.impact} users).
          </p>

          <p class="prose-measure text-base leading-relaxed text-foreground/90">
            I mostly work in TypeScript, Go, and Python. Cloudflare Workers, PostgreSQL, Docker, edge-native architectures. I like clean APIs, boring reliability, and software people actually use.
          </p>
        </div>

        <figure
          class="m-0 md:sticky md:top-[calc(4.5rem+var(--space-md))] border border-border bg-card p-2 w-full max-w-[min(100%,18rem)] md:max-w-none justify-self-center md:justify-self-auto"
        >
          <img
            src={siteConfig.authorImage}
            alt={`Portrait of ${siteConfig.name}`}
            class="w-full aspect-[4/5] object-cover object-[center_20%]"
            loading="eager"
            width="480"
            height="600"
          />
        </figure>
      </div>
    </section>

    <section class="text-band space-y-[var(--space-xl)] pb-[var(--space-2xl)]">
      <div class="space-y-[var(--space-md)]">
        <h2 class="font-display text-[length:var(--text-xl)] leading-tight text-foreground">
          Outside of code
        </h2>
        <p class="prose-measure text-base leading-relaxed text-muted-foreground">
          {siteConfig.interests}
        </p>
      </div>

      <div class="space-y-[var(--space-md)]">
        <h2 class="font-display text-[length:var(--text-xl)] leading-tight text-foreground">
          Open to work
        </h2>
        <p class="prose-measure text-base leading-relaxed text-foreground/90">
          Full-time software engineering roles (backend, infrastructure, developer tools, AI tooling) starting May/June 2026. Open to relocation and remote.
        </p>
      </div>

      <p class="prose-measure text-sm leading-relaxed text-muted-foreground">
        {siteConfig.location} · <a href={`mailto:${siteConfig.email}`} class="text-primary link-underline-grow">{siteConfig.email}</a>
      </p>

      <SocialIcons size="sm" />
    </section>

    <div class="text-band">
      <hr class="hairline-rule mb-[var(--space-2xl)]" />
    </div>

    <div class="text-band pb-[var(--space-2xl)]">
      <ExperienceBentoStatic />
    </div>

    <div class="text-band pb-[var(--space-3xl)]">
      <Skills client:visible />
    </div>
  </article>
</SiteLayout>
