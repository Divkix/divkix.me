---
import Footer from "@/components/shared/Footer.astro";
import { NavbarWithBoundary } from "@/components/shared/Navbar";
import { ScrollProgress } from "@/components/shared/ScrollProgress";
import { siteConfig } from "@/data/site.config";
import BaseLayout from "./BaseLayout.astro";

interface Props {
  title?: string;
  description?: string;
  showScrollProgress?: boolean;
  dateModified?: string;
  ogImage?: string;
  ogImageAlt?: string;
}

const {
  title = `${siteConfig.name} — Software engineer at Cloudflare, Vinext contributor`,
  description = siteConfig.seo.metaDescription,
  showScrollProgress = false,
  dateModified,
  ogImage,
  ogImageAlt,
} = Astro.props;
---

<BaseLayout
  title={title}
  description={description}
  {...(ogImage ? { ogImage } : {})}
  {...(dateModified ? { dateModified } : {})}
  {...(ogImageAlt ? { ogImageAlt } : {})}
>
  <NavbarWithBoundary client:idle />

  {showScrollProgress && <ScrollProgress client:idle />}

  <main id="main-content" class="min-h-screen">
    <slot />
  </main>

  <Footer />
</BaseLayout>
