---
phase: 01-skeleton-seo-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/validate-content.ts
  - src/content/blog/ai-models-compared-2026.mdx
autonomous: true
requirements:
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-04
  - SEO-05
  - BILD-01

must_haves:
  truths:
    - "MDX file exists at src/content/blog/ai-models-compared-2026.mdx and passes Zod schema validation during astro build"
    - "Frontmatter contains title, date, excerpt, tags, published, tldr, keyTakeaways (5), faq (5 questions), seoTitle, seoDescription"
    - "H2 headings are present for all 11 planned sections with distinct search intents"
    - "Primary keyword 'AI models compared 2026' appears in the title and first heading stub"
    - "Slug ai-models-compared-2026 passes URL-safe regex validation"
    - "validate-content.ts correctly handles published:false drafts without breaking the build"
  artifacts:
    - path: "src/content/blog/ai-models-compared-2026.mdx"
      provides: "Blog post skeleton with complete SEO frontmatter and section stubs"
      contains: "11 H2 headings, 5 FAQ entries, 5 keyTakeaways"
    - path: "scripts/validate-content.ts"
      provides: "Fixed content validation that handles unpublished drafts"
      contains: "generate-posts-metadata.js filtering logic mirrored"
  key_links:
    - from: "src/content/blog/ai-models-compared-2026.mdx"
      to: "src/content/config.ts"
      via: "Zod schema validation at build time"
      pattern: "z\\.object.*title.*date.*excerpt"
    - from: "scripts/validate-content.ts"
      to: "content/blog/posts.json"
      via: "count comparison with published-only filtering"
      pattern: "totalPosts"
---

<objective>
Create the MDX skeleton file for the "AI Models Compared 2026" blog post with complete SEO frontmatter and H2 stub headings, and fix the build validation script to handle `published: false` drafts.

Purpose: Establishes the content structure that Phase 2 and 3 will fill with actual comparison content. The SEO frontmatter (FAQ, keyTakeaways, tldr) is set now so structured data and schema are valid from the start.

Output: One new MDX file, one patched TypeScript validation script.
</objective>

<execution_context>
@/Users/divkix/.claude/get-shit-done/workflows/execute-plan.md
@/Users/divkix/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-skeleton-seo-foundation/01-CONTEXT.md
@.planning/phases/01-skeleton-seo-foundation/01-RESEARCH.md
@src/content/config.ts
@scripts/validate-content.ts
@scripts/generate-posts-metadata.js

<interfaces>
<!-- Zod schema from src/content/config.ts — the MDX frontmatter MUST satisfy this exactly -->
z.object({
  title: z.string(),                          // REQUIRED
  date: z.string().regex(/^\d{4}-\d{2}-\d{2}$/).refine(valid date), // REQUIRED, "2026-02-25"
  dateModified: z.string().optional(),        // Same format as date
  excerpt: z.string(),                        // REQUIRED
  tags: z.array(z.string()),                  // REQUIRED, non-empty
  published: z.boolean().default(false),      // Default false
  author: z.string().optional().default("Divanshu Chauhan"),
  seoTitle: z.string().optional(),
  seoDescription: z.string().optional(),
  tldr: z.string().optional(),
  keyTakeaways: z.array(z.string()).optional(),
  faq: z.array(z.object({ q: z.string(), a: z.string() })).optional(),  // NOTE: keys are q/a, NOT question/answer
  howto: z.object({ name, totalTime, steps[] }).optional(),
})

<!-- validate-content.ts line 37 — counts ALL mdx files, no published filter -->
mdxFiles = readdirSync(CONTENT_DIR).filter((f) => f.endsWith(".mdx"));

<!-- generate-posts-metadata.js line 133 — only includes published posts in posts.json -->
const publishedPosts = posts.filter((post) => post.published);
// posts.json totalPosts = publishedPosts.length (line 155)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix validate-content.ts to handle unpublished drafts</name>
  <files>scripts/validate-content.ts</files>
  <action>
The user locked `published: false` for this draft post (CONTEXT.md decision). But `validate-content.ts` counts ALL `.mdx` files (line 37) while `generate-posts-metadata.js` only writes published posts to `posts.json` (line 133). This causes a count mismatch that fails the build.

Fix `validate-content.ts` to read each MDX file's frontmatter and only count published posts when comparing against `posts.json`. Specifically:

1. Add `import matter from "gray-matter";` at the top (gray-matter is already a project dependency, used by `generate-posts-metadata.js`).

2. After getting `mdxFiles` (line 37), filter to only published posts:
   ```typescript
   const publishedMdxFiles = mdxFiles.filter((f) => {
     const content = readFileSync(join(CONTENT_DIR, f), "utf-8");
     const { data } = matter(content);
     return data.published !== false;
   });
   ```

3. Use `publishedMdxFiles` (instead of `mdxFiles`) for:
   - The count comparison on line 62: `publishedMdxFiles.length !== postsJson.totalPosts`
   - The slug extraction on line 45: derive `mdxSlugs` from `publishedMdxFiles` instead of `mdxFiles`

4. Keep the slug format validation (line 86) running against ALL mdx files (published and unpublished) — slug validation should still catch bad slugs in drafts.

5. Add a log line after the published filter: `console.log(\`📝 Found ${mdxFiles.length} MDX files (${publishedMdxFiles.length} published, ${mdxFiles.length - publishedMdxFiles.length} drafts)\`);`

Do NOT change the gray-matter import to a dynamic import — use the standard CommonJS-style import that matches the project's other scripts. Check existing imports in the file for the correct pattern (the file uses `import { existsSync, readdirSync, readFileSync } from "node:fs"` style).
  </action>
  <verify>
    <automated>cd /Users/divkix/GitHub/divkix.me && bun run lint && bun run type-check</automated>
  </verify>
  <done>
    - validate-content.ts reads frontmatter from each MDX file using gray-matter
    - Only published posts are counted when comparing against posts.json
    - Unpublished drafts are excluded from count/slug comparison but still slug-validated
    - Lint and type-check pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Create MDX skeleton with complete SEO frontmatter and H2 stubs</name>
  <files>src/content/blog/ai-models-compared-2026.mdx</files>
  <action>
Create `src/content/blog/ai-models-compared-2026.mdx` with the following content. Follow CONTEXT.md locked decisions exactly.

**Frontmatter (YAML between --- delimiters):**

```yaml
title: "AI Models Compared 2026: I Tested All Five Flagships So You Don't Have To"
seoTitle: "AI Models Compared 2026: Claude vs GPT vs Gemini vs Grok (Honest Review)"
seoDescription: "Hands-on comparison of Claude Opus 4.6, GPT 5.2, Codex 5.3, Gemini 3.1 Pro, and Grok 4.20 across coding, reasoning, agents, writing, and multimodal tasks with benchmarks."
date: "2026-02-25"
excerpt: "Five flagship AI models. Three months of daily use. One spreadsheet of benchmarks. Here's the honest breakdown — and the winner wasn't what I expected."
tags:
  - AI
  - Claude Opus
  - GPT 5
  - Gemini
  - Grok
  - AI Models
  - AI Comparison
  - Coding
  - "2026"
  - Best AI Model
published: false
tldr: "No single AI model wins everything. Claude Opus 4.6 dominates coding, Gemini 3.1 Pro leads multimodal and reasoning, GPT 5.2 is the best all-rounder for everyday use, and Grok 4.20's multi-agent architecture is the most innovative approach — but still in beta."
keyTakeaways:
  - "Claude Opus 4.6 is the coding king — 80.8% SWE-bench Verified, unmatched for multi-file refactors and agentic workflows"
  - "GPT 5.2 is the safest all-rounder — solid everywhere, exceptional nowhere, but the unified router picks the right model for you"
  - "Gemini 3.1 Pro owns multimodal and long-context — 10M token window and 77.1% ARC-AGI-2 make it the reasoning/research leader"
  - "Grok 4.20's 4-agent debate system is genuinely innovative — Harper, Benjamin, Lucas, and Captain Grok reduce hallucinations by 65%"
  - "Codex 5.3 xHigh is the dark horse for pure code generation — 89% LiveCodeBench but limited to coding-only workflows"
faq:
  - q: "What's the best AI for coding in 2026?"
    a: "Claude Opus 4.6 leads with 80.8% on SWE-bench Verified, excelling at multi-file refactors and agentic terminal workflows. For pure code generation speed, Codex 5.3 xHigh scores 89% on LiveCodeBench."
  - q: "Which AI model is best for everyday use in 2026?"
    a: "GPT 5.2 with its internal router that auto-selects the right model per request. It's consistently good across all tasks without requiring you to pick a specialized model."
  - q: "Is Gemini better than ChatGPT in 2026?"
    a: "For research and long documents, yes — Gemini 3.1 Pro's 10M token context and 77.1% ARC-AGI-2 score beat GPT 5.2. For general daily use and writing, GPT 5.2 edges ahead."
  - q: "What is Grok 4.20 and how does it work?"
    a: "Grok 4.20 is xAI's multi-agent system where four specialized AI agents (Grok, Harper, Benjamin, Lucas) debate each other before giving you a unified answer, reducing hallucinations by 65%."
  - q: "Claude vs GPT vs Gemini: which AI should I use in 2026?"
    a: "It depends on your task. Claude for coding, Gemini for research and multimodal, GPT for all-around daily use, Grok for cutting-edge multi-agent reasoning. No single model wins everything."
```

**MDX Body (after frontmatter):**

11 H2 stub headings with MDX comment annotations indicating which phase/requirement fills each section. Include the primary keyword "AI models compared 2026" in the first line of body text (a stub paragraph before the first H2). The headings must use the exact category labels from CONTEXT.md locked decisions.

```mdx
AI models compared 2026 — this is the no-BS breakdown after three months of daily use with every major flagship.

## The Five AI Models You Actually Need to Know About

{/* Hook section — Phase 3, STRC-01 */}

## Quick Comparison: AI Models at a Glance

{/* Comparison table — Phase 3, STRC-02 */}

## Meet the Contenders

{/* Model introductions — Phase 3, STRC-03 */}

## Coding & Software Engineering

{/* Head-to-head — Phase 2, STRC-04 */}

## Reasoning & Complex Problem Solving

{/* Head-to-head — Phase 2, STRC-05 */}

## Agents & Tool Use

{/* Head-to-head — Phase 2, STRC-06 */}

## Creative Writing & Communication

{/* Head-to-head — Phase 2, STRC-07 */}

## Multimodal: Vision, Audio, Video

{/* Head-to-head — Phase 2, STRC-08 */}

## The Grok 4.20 Spotlight: AI That Argues With Itself

{/* Grok spotlight — Phase 2, STRC-09 */}

## Benchmark Scorecard: The Numbers Behind the Hype

{/* Scorecard table — Phase 2, STRC-10 */}

## The Anti-Hype Check: What the Benchmarks Don't Tell You

{/* Anti-features + benchmark gaming caveat — Phase 2, QUAL-02, QUAL-03 */}

## The Verdict: Which AI Model Should You Actually Use?

{/* Segmented verdict — Phase 3, STRC-11 */}
```

**Discretion decisions (documenting choices):**
- TL;DR tone: Factual with slight casual edge per research recommendation — information density over personality
- Section ordering: Hook → Comparison Table → Intros → 5 Categories → Grok Spotlight → Scorecard → Anti-Hype → Verdict (inverted pyramid)
- Anti-Hype as standalone H2 (not folded into verdict) — targets "AI benchmark accuracy" search intent
- Tags: model names + categories + year (matches existing blog patterns)
- Grok FAQ question included ("What is Grok 4.20 and how does it work?") — high search interest
- Excerpt already differentiates from ai-coding-tools-compared-2026 via broader scope framing
- FAQ answer length varies: 1-2 sentences for "best for X" questions, 2 sentences for comparison questions

**Important:** Use `published: false` per the user's locked decision in CONTEXT.md. Task 1 fixes the validation script to handle this correctly.
  </action>
  <verify>
    <automated>cd /Users/divkix/GitHub/divkix.me && bun run prebuild && bun run type-check</automated>
  </verify>
  <done>
    - MDX file exists at src/content/blog/ai-models-compared-2026.mdx
    - Frontmatter passes Zod schema validation (astro check succeeds)
    - prebuild completes: posts.json regenerated (excludes this draft), validate-content passes (counts match)
    - Title contains "AI Models Compared 2026"
    - 5 keyTakeaways present (one per model)
    - 5 FAQ entries with q/a keys targeting real search queries
    - 11 H2 stub headings present with distinct search intents
    - Slug ai-models-compared-2026 passes /^[a-z0-9-]+$/ regex
    - published is false (user locked decision)
  </done>
</task>

</tasks>

<verification>
Run the full validation chain to confirm everything works together:

1. `bun run lint` — Biome linting passes for validate-content.ts (MDX excluded from Biome)
2. `bun run type-check` — astro check confirms Zod schema validation passes for the new MDX file
3. `bun run prebuild` — generates posts.json (excluding the draft) and validate-content passes (published counts match)
4. Verify slug format: filename `ai-models-compared-2026.mdx` produces slug `ai-models-compared-2026` which matches `/^[a-z0-9-]+$/`
5. Verify keyword placement: "AI models compared 2026" appears in title and in the first 100 words of body text
6. Count H2 headings in MDX body: should be exactly 11
7. Count FAQ entries: should be exactly 5
8. Count keyTakeaways: should be exactly 5
</verification>

<success_criteria>
- `bun run prebuild` passes with no errors (posts.json sync validated)
- `bun run type-check` passes (Zod schema satisfied)
- `bun run lint` passes (no Biome violations)
- MDX file at correct path with published: false
- All 6 phase requirements (SEO-01 through SEO-05, BILD-01) addressed
</success_criteria>

<output>
After completion, create `.planning/phases/01-skeleton-seo-foundation/01-01-SUMMARY.md`
</output>
