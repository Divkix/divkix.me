---
phase: 05-integration-fixes-content-corrections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/blog/ai-models-compared-2026.mdx
autonomous: true
requirements:
  - SEO-01
  - QUAL-04

must_haves:
  truths:
    - "JSON-LD BlogPosting.dateModified in built HTML reads 2026-02-26T00:00:00Z, not 2026-02-25"
    - "article:modified_time meta tag exists in built HTML head with content 2026-02-26T00:00:00Z"
    - "Cross-reference link href ends with trailing slash (/blog/ai-coding-tools-compared-2026/)"
    - "No occurrence of '10M token' remains in keyTakeaways or FAQ answer text"
    - "bun run build completes with exit code 0"
  artifacts:
    - path: "src/content/blog/ai-models-compared-2026.mdx"
      provides: "Corrected frontmatter (dateModified, 1M token) and body (trailing slash)"
      contains: "dateModified"
  key_links:
    - from: "src/content/blog/ai-models-compared-2026.mdx (dateModified frontmatter)"
      to: "dist/blog/ai-models-compared-2026/index.html (JSON-LD + meta tag)"
      via: "[slug].astro dateModified conditional -> BaseLayout article:modified_time"
      pattern: "dateModified.*2026-02-26"
---

<objective>
Fix all integration gaps and factual errors from the v1.0 milestone audit: add dateModified to frontmatter, correct "10M -> 1M" token references in keyTakeaways and FAQ, and add trailing slash to cross-reference link. Then rebuild and verify the fixes propagate to built HTML.

Purpose: Closes INT-01 (dateModified inconsistency), INT-02 (trailing slash redirect), and FLOW-01 (SEO date consistency) from the milestone audit. These are the last remaining items before the milestone is fully clean.
Output: Corrected MDX file + clean build with verifiable HTML output.
</objective>

<execution_context>
@/Users/divkix/.claude/get-shit-done/workflows/execute-plan.md
@/Users/divkix/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-integration-fixes-content-corrections/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply four content corrections to MDX file</name>
  <files>src/content/blog/ai-models-compared-2026.mdx</files>
  <action>
Make four edits in `src/content/blog/ai-models-compared-2026.mdx`:

**Edit 1 — Add dateModified to frontmatter (after line 5):**
Insert `dateModified: "2026-02-26"` immediately after the existing `date: "2026-02-25"` line. Use quoted YYYY-MM-DD format (matches Zod schema regex `/^\d{4}-\d{2}-\d{2}$/` in content/config.ts). Do NOT use ISO 8601 with time component.

Before:
```yaml
date: "2026-02-25"
excerpt: "Five flagship AI models..."
```

After:
```yaml
date: "2026-02-25"
dateModified: "2026-02-26"
excerpt: "Five flagship AI models..."
```

**Edit 2 — Fix keyTakeaways[2] (line 23):**
Change `10M token window` to `1M token context window`.

Before: `"Gemini 3.1 Pro owns multimodal and long-context — 10M token window and 77.1% ARC-AGI-2 make it the reasoning/research leader"`
After: `"Gemini 3.1 Pro owns multimodal and long-context — 1M token context window and 77.1% ARC-AGI-2 make it the reasoning/research leader"`

**Edit 3 — Fix FAQ answer[2] (line 32):**
Change `10M token context` to `1M token context`.

Before: `"For research and long documents, yes — Gemini 3.1 Pro's 10M token context and 77.1% ARC-AGI-2 score beat GPT 5.2..."`
After: `"For research and long documents, yes — Gemini 3.1 Pro's 1M token context and 77.1% ARC-AGI-2 score beat GPT 5.2..."`

**Edit 4 — Add trailing slash to cross-reference link (line 190):**
Change `/blog/ai-coding-tools-compared-2026)` to `/blog/ai-coding-tools-compared-2026/)` (append slash before closing paren).

Before: `(/blog/ai-coding-tools-compared-2026)`
After: `(/blog/ai-coding-tools-compared-2026/)`

**Do NOT** edit any template files ([slug].astro, BaseLayout.astro, BlogLayout.astro). The templates already handle dateModified correctly — the gap was purely missing data.
**Do NOT** edit posts.json directly — it is a generated artifact.
  </action>
  <verify>
    <automated>grep -c "dateModified" src/content/blog/ai-models-compared-2026.mdx | grep -q "1" && grep -c "10M token" src/content/blog/ai-models-compared-2026.mdx | grep -q "^0$" && grep -c "ai-coding-tools-compared-2026/" src/content/blog/ai-models-compared-2026.mdx | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>MDX file contains dateModified: "2026-02-26", zero occurrences of "10M token", and cross-reference href ends with trailing slash</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild and verify HTML output</name>
  <files>src/content/blog/ai-models-compared-2026.mdx</files>
  <action>
Run `bun run build` (which includes prebuild steps: generate-posts-metadata.js, generate-og-images.js, validate-content.ts, then astro build). The build must complete with exit code 0.

After build, verify four things in `dist/blog/ai-models-compared-2026/index.html`:

1. **JSON-LD dateModified**: `"dateModified":"2026-02-26T00:00:00Z"` present in the BlogPosting JSON-LD script tag
2. **article:modified_time meta tag**: `<meta property="article:modified_time" content="2026-02-26T00:00:00Z"` present in the head
3. **Cross-reference trailing slash**: The href to ai-coding-tools-compared-2026 ends with `/` before the closing quote
4. **No 10M token text**: Zero matches for "10M token" in the rendered HTML

If any of these four checks fail, the task is not done — re-examine the MDX edits.
  </action>
  <verify>
    <automated>bun run build && grep -q '"dateModified":"2026-02-26T00:00:00Z"' dist/blog/ai-models-compared-2026/index.html && grep -q 'article:modified_time' dist/blog/ai-models-compared-2026/index.html && grep -q 'ai-coding-tools-compared-2026/' dist/blog/ai-models-compared-2026/index.html && ! grep -q '10M token' dist/blog/ai-models-compared-2026/index.html && echo "ALL CHECKS PASS" || echo "VERIFICATION FAILED"</automated>
  </verify>
  <done>Build succeeds. Built HTML contains dateModified 2026-02-26T00:00:00Z in JSON-LD and article:modified_time meta tag, cross-reference link has trailing slash, and no "10M token" text appears anywhere in the rendered page.</done>
</task>

</tasks>

<verification>
All four gap closures verified in built HTML output:
1. INT-01 (dateModified): JSON-LD and meta tag both emit 2026-02-26T00:00:00Z
2. INT-02 (trailing slash): Cross-reference href includes trailing slash, no 301 redirect
3. FLOW-01 (SEO date consistency): dateModified consistent across frontmatter, JSON-LD, meta tag, and sitemap
4. Tech debt items 1-2 (10M -> 1M): Corrected in keyTakeaways and FAQ
5. Build pipeline: `bun run build` exits 0
</verification>

<success_criteria>
- `bun run build` completes without errors
- `grep '"dateModified":"2026-02-26T00:00:00Z"' dist/blog/ai-models-compared-2026/index.html` returns a match
- `grep 'article:modified_time' dist/blog/ai-models-compared-2026/index.html` returns a match
- `grep 'ai-coding-tools-compared-2026/' dist/blog/ai-models-compared-2026/index.html` returns a match
- `grep '10M token' dist/blog/ai-models-compared-2026/index.html` returns NO matches
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-fixes-content-corrections/05-01-SUMMARY.md`
</output>
