# Contracts: N/A

This feature is a blog post (internal content). No external interfaces, APIs, CLIs, or public contracts are exposed. The only 'contract' is the MDX frontmatter conforming to the Zod schema in `src/content/config.ts`, which is documented in the data model.
